def div_check(x):
    for n in range (1, 21):
        if x % n == 0:
            continue
        else:
            return True
    return False

count = 20

while div_check(count) == True:
    count += 2

print(count)
